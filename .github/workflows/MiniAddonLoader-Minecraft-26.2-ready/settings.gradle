pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        gradlePluginPortal()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        mavenCentral()
    }
}
rootProject.name = "mini-addon-loader"
include("core", "example-addon")
