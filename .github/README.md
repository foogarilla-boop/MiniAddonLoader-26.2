# MiniAddonLoader — Minecraft 26.2

This project is prepared for a GitHub Actions build using Java 25.

## Build
GitHub Actions uses `.github/workflows/build.yml`.

## Important
The Gradle Wrapper files (`gradlew`, `gradlew.bat`, and `gradle/wrapper/*`) must be present before the workflow can run.
